# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
APP_URL="https://ais-dev-hegt44hrnin34wosgopvmc-563762342057.asia-southeast1.run.app"

# OWNER_EMAIL: The Google email address designated as the Store Owner / Administrator.
# Default store owner email (Only this email gets full Owner/Admin panel access):
OWNER_EMAIL="vmanjeet773@gmail.com"

# OWNER_PASSWORD: Secure backend secret password for the store owner.
# Configured securely through server-side environment variables / secrets.
OWNER_PASSWORD=""

# Google OAuth Credentials (for Google Sign-In with OAuth popups):
# Setup in Google Cloud Console: Credentials -> OAuth 2.0 Client ID (Web Application)
# Authorized redirect URI: <APP_URL>/auth/callback
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# JWT / Secret Key for Signed Short-Lived Download Tokens (Backend only):
DOWNLOAD_SECRET="digivault_secure_signed_token_vault_key_2026"
